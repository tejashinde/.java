/*
* note : all the classes listed below are present in the java.io package
Reader Class (Abstract)
    - FileReader reads one chatracter at a time i.e 2 bytes
    - Input stream reads 1 byte at a time
    - BufferedReader gathers a chunk of data, buffers the characters
        - LineNumberReader
    - CharArrayReader reads characters from character array
    - FileReader
Writer Class (Abstract)
    - FileWriter will write or send one character at a time to the destination
    - BufferedWriter will gather chunk of characters and send to destincation
    - CharArrayWriter writes or sends data to the character array
    - StringWriter writes data to the stream
    - FileWriter
*/
