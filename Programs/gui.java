import java.awt.*;
import java.swing.*;
import java.awt.event.*;
class gui
{
    JFrame f = new JFrame("GUI");
    Label lbl = new Label("Registration form");
    f.add(lbl);
    f.setSize(500,500);
    f.setVisible(true);
    f.addWindowListener(new WindowAdapter()){
        public void windowClosijn
    }
}